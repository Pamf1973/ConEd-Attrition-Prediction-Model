---
generated_at: 2026-08-17T00:15-0400
covers_sessions: 2026-08-14 → 2026-08-17
next_session_opens_with: read this file → /brief
---

# Handoff — 2026-08-17 (post 7 PM call)

## Post-call updates (2026-08-16 evening call, captured 2026-08-17 00:15)

**MERGE CHAIN LANDED.** #10 → #11 → #12 all on main. Plus Ismael pushed one follow-up (`5a6520a`) guarding `sanitizeNote` against null/non-string input. Head of main is now `5a6520a`.

Conflict resolution notes from Ismael:
- `model_meta` (#11) + status endpoints (#10) were in the same spot in `api/server.js` — both kept.
- Auth comparison: kept SHA-256 hash-both-sides from main (same security property as the HMAC approach on the branch, simpler).
- `package.json` / `package-lock.json`: took main's (`pg` already there from #10).

**Team scope clarified.** Pedro is scoped to deployment/Railway only. Building work is Edwin + Ismael from here on. M3 pickup already reflects this — Ed owns M3 solo, Ismael offered to design-pass PRs #15/#16 when Ed's ready to pull him in.

**Unblocked as of merge chain landing:**
- `edwin/M1-fallback-cleanup` PR (pre-staged at `docs/notes/2026-08-16_pr11-post-merge-cleanup.md`) — fire whenever ready. `data/model_meta.json` is now live on main with real values (`cv_auc: 0.6833`, `n_positive: 54`), so the fallback literals are safe to retire.
- M4 case-file — `/api/model_meta` is now on main, fixture-first pattern can wire against real endpoint.
- PR #16 rebase — auto-flips base to main when #15 merges.

**Still open on Ed:**
- Sunday touchpoint time (Ismael still asked in the same message).
- Design-pass invite to Ismael on PRs #15/#16 whenever Ed wants to flip them out of DRAFT.
- Zoom recording reply (this week).
- Both Ismael check-ins (R7 watchlist, `plans/ai_model_config.md`) — still staged in `docs/notes/2026-08-16_ismael-checkins-drafts.md`, still worth sending given tomorrow's asymmetry (Ismael's active, drafts are ready).

## Where the build stands (post-merge)

| Milestone | Status |
|---|---|
| M0 (routing + legacy separation) | Shipped on main |
| M1 (model_meta.json + /api/model_meta) | Shipped on main via #11 |
| M2 (AUC rerun) | AUC 0.6833 in `data/model_meta.json` on main |
| M3 (score cell + rankings) | PRs #15/#16 open DRAFT, Edwin-owned |
| M6 backbone (status events) | Shipped on main via #10 |
| M6 watchlist migration | Still on Ismael, no PR |
| Q7/W1/W4/W6 frontend | Shipped on main via #12 |
| M4 case-file | Unblocked, unstarted |
| M10 methodology | First draft on `edwin/M10-methodology`, WIP no PR |

## Next-session opening

```
read docs/notes/2026-08-16_handoff.md
/brief
```

Then likely first action: file `edwin/M1-fallback-cleanup` PR (pre-diffed and ready), followed by starting M4 fixture-first, and either flipping PRs #15/#16 out of DRAFT with a design-pass invite to Ismael or continuing M10 authoring.

---

# Handoff — 2026-08-16 evening (pre-call, preserved)

Living pickup doc. Overwritten at end of each session. Complements PROJECT_STATE (structural state) and STATE_LOG (append-only history) — this file captures **what we personally did**, **what teammates did off-git or on PR branches**, and **what to pick up next session**. Slack context lives here because /brief can't see Slack.

---

## Next session — open with this

```
read docs/HANDOFF.md
/brief
```

The addendum below the horizontal rule is the "since last time" summary /brief can't reconstruct on its own (Slack activity, PR-branch commits not on main, in-flight decisions). Skim it, then let /brief verify git state.

**If tonight's merge chain ran successfully:** /brief will show PROJECT_STATE stale, state-sync will run, and #10/#11/#12 will land as merged. Next task: file the `edwin/M1-fallback-cleanup` PR (staged at `docs/notes/2026-08-16_pr11-post-merge-cleanup.md`), then start M4 fixture-first.

**If the merge chain didn't run:** /brief will surface it. Ping Ismael, don't start M4 (M4 depends on `/api/model_meta` from #11).

---

## What teammates did (2026-08-14 → 2026-08-16)

### Pedro
- **2026-08-14** — provisioned new Railway service on his paid account (new prod URL). Old Mel-account URL retired.
- **2026-08-16 00:04** — added `ACTOR_HMAC_SECRET` to Railway env, deploy successful. Asked Ed to verify app is still up before Ismael proceeds with merges.
- **2026-08-16** — requested 15-min Sunday touchpoint (2026-08-17). Time unconfirmed; Ed offered 10 AM ET.

### Ismael
- **2026-08-16 morning** — pushed D11 partial-revert as `388a466` on `ismael/pr-9c-frontend-workflow`: composite_risk demoted to secondary, `risk` back to `ml_risk` primary, `src/legacy/` fully reverted, labels restored, `/api/health` auth fixed, PR #11 body updated to `data/model_meta.json`. All 6 asks addressed.
- **2026-08-16** — self-directed security + code review pass, pushed as `1e51a3f`:
  - `/api/health` was leaking AI provider name to unauth → stripped to `{ok: true}`
  - `/api/predict/custom` `steam_decline` used raw `weights` not `safeWeights` (null-body throw)
  - Composite formula was zeroing ml_risk factor for no-XGBoost buildings → now excluded from weighted average
  - `composite_risk` BuildingPanel row rendering for all buildings (dead null-guard) → gated on `has_ml_risk`
  - Conflict indicator labeled heuristic scores as "ML:" for non-XGBoost → suppressed
- **2026-08-16** — third pre-merge panel pass, pushed as `1627643`: `child.stdin` no error handler in `/api/predict/live` → EPIPE crash risk. Added no-op handler.
- **2026-08-16 14:06** — Slack: in class until 6 PM ET, can only listen. Will run merge chain post-6 PM.
- **Panel report:** PR #10, #11, #12 all clean.

### Mel
- Silent this cycle. D10 env-var migration to Pedro's Railway is partially done via Pedro's direct pastes.

---

## What we personally did (Edwin + assistant)

### Session 1 (2026-08-14 → 2026-08-15)
- `/sync` against 08e1573; reconciled pre-existing dirty tree with new Slack activity; PROJECT_STATE rewritten, STATE_LOG appended.
- **D11 filed** (`7a640a4`) rejecting composite-primary flip on PR #12. Anchored to M3 spec + §7 rule 7 + §8 rule 1 + D7.
- **PR #12 review** — posted CHANGES_REQUESTED with surgical revert plan (keep composite as secondary, restore `risk = ml_risk`, revert `src/legacy/`, restore labels). Ismael executed all six asks as `388a466`.
- **PR #11 approval** on 98a676a. Verified 3 requested copy fixes landed. Flagged PR body doc drift (`public/` → `data/`) + fallback AUC nit.
- **Prod smoke test** on new Railway URL — `/`, `/legacy`, deep-link refresh, login round-trip all pass. Flagged `/api/health` auth drift (Ismael later fixed).
- Generated `ACTOR_HMAC_SECRET` (32-byte hex) and sent to Pedro via DM with step-by-step Railway instructions.

### Session 2 (2026-08-16 afternoon → evening)
- **Prod re-verify** post-Pedro env-var paste (2026-08-16 16:17). App up, login clean, data endpoints authing. PR #10 re-approved with prod-verified note. Slack go-ahead sent to Ismael.

- **M3 Score Cell — shipped as stacked PRs:**
  - **PR #15** (`edwin/M3-score-cell`, base main): score cell atom + `/m3-preview` route. Files: `src/next/ScoreCell.{jsx,css}`, `src/next/scoreCellStates.js` (6-state fixture matrix per anatomy §03), `src/next/M3Preview.{jsx,css}`, route mount in `src/main.jsx`, Google Fonts link in `index.html`. L1 enforced by string-typed percentile prop; unknown tier throws. Provenance vocabulary uses v1.1 XGB strings — intentional spec deviation from anatomy's Round-0 GBM strings.
  - **PR #16** (`edwin/M3-rankings-container`, base `edwin/M3-score-cell`): rankings container wiring ScoreCell to real building data via `src/next/scoreCellAdapter.js`. Route `/rankings`. Adapter: `baseTier` (<0.2 Low, ≥0.6 High), portfolio percentile with tie-sharing, `freshnessChip` from `norm_delta_23_24`/`norm_delta_22_23`, S5 fallback for `!has_ml_risk`, divergence flag only on base=Low + tier=High (L3 v1.1 two-tier). Reads `sessionStorage.coned_token`. Container PR auto-flips base to main when #15 merges.
  - Preview verified in browser. Both PRs DRAFT, self-ownable.

- **M10 methodology — first draft committed + pushed WIP:**
  - Branch `edwin/M10-methodology`, commit `e4516a9`. Pushed to origin without opening PR.
  - Files: `src/next/MethodologyPage.{jsx,css}`, route mount at `/methodology`. 550 insertions.
  - Nine-section report register. Sections 1, 3, 5, 6, 7 fully drafted from `system-v1.1.md` + `docs/ref/2026-07-16_methodology-alignment.md`. Sections 2, 4, 8, 9 rendered as clearly-labeled `.mp-placeholder` blocks (regenerates per model version / per pipeline run / research pending).
  - Report register tokens (paper #F8F8F6, ink #17181A, Space Grotesk display, IBM Plex Mono meta), per-section dual-clock stamps, print media query.
  - **Prose is scaffolding for Edwin's authoring pass.** Not review-ready. Revise register + confirm §3 distribution facts (70%/78%/176) against current pipeline before opening a PR.

- **Post-merge cleanup staged** at `docs/notes/2026-08-16_pr11-post-merge-cleanup.md`. Three drift sites identified (validator default `cv_auc: 0.68`, read-failure fallback object, `getAnswer` `?? 0.68`). Proposed diff: all three default to null; `getAnswer` branches on null and emits §7 rule 8 interim sentence. Ready-to-fire PR body included. Branch: `edwin/M1-fallback-cleanup`.

- **Ismael check-in drafts staged** at `docs/notes/2026-08-16_ismael-checkins-drafts.md`. Two async messages: (1) R7 watchlist migration status, (2) `plans/ai_model_config.md` status. For Ed to send post-6 PM ET.

- **Decisions surfaced:**
  - Model work deferred until post-redesign (saved as memory `project_model_work_deferred.md`).
  - Ed committed to answering Zoom recording ask this week.
  - Sunday touchpoint time still unconfirmed.

- **PROJECT_STATE updated** (this session, pre-handoff) to reflect Slack-side truth: Pedro env-var cleared, Ismael's PR-branch commits reconciled, PR #12 marked APPROVED, stale open commitments removed, risks rewritten.

---

## Live blockers snapshot (as of 2026-08-16 19:30)

| Item | Blocking | Owner | Status |
|---|---|---|---|
| Merge chain #10 → #11 → #12 | M4 case-file, cleanup PR, rebase of PRs #15/#16 base | Ismael | Runs post-6 PM ET tonight |
| PR #15/#16 flip DRAFT → OPEN | landing to main | Edwin | Optional review pull-in first |
| M10 PR open | none (WIP branch parked) | Edwin | Authoring pass first |
| Zoom recording ask reply | none | Edwin | This week |
| Sunday touchpoint time | Sunday sync | Pedro + Ismael | Awaiting responses |

---

## Where things live

- **PROJECT_STATE.md** — current structural state (built, in flight, blocked, commitments, risks). Rewritten by /sync.
- **STATE_LOG.md** — append-only per-cycle history. Never rewritten.
- **DECISIONS/D*.md** — durable spec decisions (D11 = reject composite-primary flip).
- **ROADMAP.md** — R1–R14 dependency graph. Milestone anchors: R2/R3 = M1, R4 = M3, R5 = M4, R7 = M6, R11 = M10.
- **docs/notes/YYYY-MM-DD_*.md** — Slack transcriptions, session closes, staged notes. Search here for off-git context.
- **docs/HANDOFF.md** — this file. Overwritten per session.

---

## What this file is NOT

- Not authoritative state — PROJECT_STATE is.
- Not history — STATE_LOG is.
- Not a decision record — DECISIONS/ is.
- Not a task list — that's ephemeral per session.

This is the tour guide that hands the next session what git and PROJECT_STATE alone can't.

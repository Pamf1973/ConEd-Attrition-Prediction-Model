---
date: 2026-08-17 14:10 EDT
author: Edwin (via Claude Code)
purpose: Hand off mid-M4 state to the next session cleanly.
---

# Handoff — 2026-08-17 (post-regex-fix, post-M4 build, pre-eyeball)

## Where we left off

- `main` is at `b1efee2` (Ismael's AUC-literal fixes + `plans/ai_model_config.md` committed).
- Regex crash from prior session fixed and merged (PR #18 → `462c7d1`).
- `package-lock.json` refresh committed to main (`8994999`).
- **M4 atom + container are built, rebased, pushed — but not yet opened as PRs.**
- **Not yet eyeballed in browser.** Dev server can boot without Postgres now (PR #19).

## Open PRs (as of 14:10 EDT)

| # | Branch | State | Status |
|---|---|---|---|
| 15 | `edwin/M3-score-cell` | Open | Fix pushed (`b45a232` — dead ternary removed on line 43). Waiting on Ismael to merge. |
| 16 | `edwin/M3-rankings-container` | Open | Approved. Auto-rebases when #15 merges. |
| 19 | `edwin/dev-db-non-fatal` | Open | Filed today. Makes `initSchema()` non-fatal outside production so `npm run dev` works without local Postgres. Verified locally. |

## Branches not yet opened as PRs

| Branch | Head | Base | Purpose |
|---|---|---|---|
| `edwin/M4-case-file-atom` | `a117630` | `main` | CaseFileHeader component + `/m4-preview` route with 4 fixtures |
| `edwin/M4-case-file-container` | `b737df0` | `edwin/M4-case-file-atom` | Container adapter + `/case-file/:bbl` route, wires atom to real data |

Both rebased onto latest main. **Container branch was reset from a temp local merge of `edwin/dev-db-non-fatal` (commit `66d0fab`) — that merge was for eyeball only and is now gone.** Origin matches local.

## The Ismael reconciliation

Ismael's message earlier today (10:56 AM per his Slack):
- Committed two more AUC-literal fixes on main + `plans/ai_model_config.md` (`b1efee2`). Panel clean.
- M3 design pass done: PR #15 needed a one-line fix (dead ternary `${tickClass[tier] ? "" : ""}` on `ScoreCell.jsx:43`). PR #16 approved as-is.
- Verbatim: *"Fix the one line on #15 and both are good to merge."*

We fixed and pushed. Waiting for his merge.

## What's next (in order)

1. **You eyeball M4 in browser.**
   - Boot: `npm run dev` (works without Postgres now that PR #19 is on `edwin/dev-db-non-fatal` — but until #19 merges, either check out that branch or merge it locally-only for testing).
   - Preview fixtures: `http://localhost:5173/m4-preview` (or whatever port Vite lands on)
   - Real data: `http://localhost:5173/case-file/1013370001` — needs auth. Log in first at `/legacy`, that puts a token in `sessionStorage.coned_token` which the container reads.
   - What to check: identity row layout, 3-col ledger tick colors + tier words, driver band diverging bars, narrative slot (should render "designed empty" frame for buildings without narrative), status segment (all 6 states visible read-only), warn banner when status endpoint 500s.

2. **Open M4 PRs as DRAFT.** Two stacked PRs:
   - Atom → main, title: `feat(M4): case-file header atom + preview route`
   - Container → atom, title: `feat(M4): case-file container + adapter wiring atom to real data`
   - Body should list which H1–H5 laws are satisfied and cite the Fable spec 2 anatomy HTML at `docs/ref/2026-07-16_fable-case-file-header.html`.

3. **Wait on Ismael for:**
   - Merge PR #15 (unblocks #16)
   - Merge PR #16
   - Review PR #19 (dev-non-fatal)

4. **Once #15/#16/#19 all land**, flip M4 PRs out of draft and add Ismael as reviewer.

## Explicit deferrals (do not do now)

- **Status write wiring.** M4 container is read-only against `/api/buildings/:bbl/status` GET. POST wiring is deferred to a follow-up PR per Sunday scope call. Don't scope-creep this into the M4 container PR.
- **`/api/model_meta` auth.** Container reads from `sessionStorage.coned_token`. Login flow itself lives on `/legacy` until M9. Do not move login into the new-build routes yet.

## Files touched this session (not on origin PRs yet — only local main writes)

None. All work is on branches. Nothing dirty on main locally.

## Environment notes for next session

- **Node 22** is the local runtime. The regex fix (PR #18) was Node-22-parseable; the `sanitizeNote` character class uses `\u` escapes now.
- **No local Postgres.** PR #19 handles this — until it merges, either check out `edwin/dev-db-non-fatal` or expect `[db] FATAL:` on `npm run dev`.
- **Vite port drift.** Prior dev runs left 5173/5174 held; Vite fell back to 5175. Check output for actual port. Run `pkill -f vite` before booting fresh if unsure.
- **`.ml_venv` missing** locally — `/api/predict/live` will fail. Not needed for M4 eyeball.

## Cross-refs

- `system-v1.1.md` §5 (case-file header component), §7 rules 8/9 (AUC + model_version templating)
- `docs/ref/2026-07-16_fable-case-file-header.html` (visual anatomy the atom ports)
- `docs/notes/2026-08-16_handoff.md` (yesterday's handoff — what led here)
- `docs/notes/2026-08-16_ismael-checkins-drafts.md` (R7 + ai_model_config asks — both resolved)
- `PROJECT_STATE.md` (last synced to `20a5876`, one sync cycle behind — safe to /sync at session start)
